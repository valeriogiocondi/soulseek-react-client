let server = 'http://localhost:3001/';
let login = server+'login/';
let search = server+'search/';
let download = server+'download/';

let URLs = {

	server: {
		main: server,
		login: login,
		search: search,
		download: download,
	}
};

module.exports = URLs;